version: "3.9"
services:
  cris:
    image: "ghcr.io/heg-incipit/incipit-cris:main"
    ports:
      - "8000:8000"
    volumes:
      - db:/var/lib/mysql
      - fuseki:/app/fuseki

volumes:
  db:
  fuseki: